import { chain } from 'lodash';
var DEFAULT_PROPERTIES = ['label', 'postalcode', 'postcode'];
export function getGeometryGeocodedString(geometryReverseGeocoded) {
  var properties = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : DEFAULT_PROPERTIES;
  if (!geometryReverseGeocoded) return 'Unknown Location';
  return chain(geometryReverseGeocoded).pick(properties).values().compact().join(', ').value();
}
//# sourceMappingURL=index.js.map