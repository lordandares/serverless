{
  "name": "@lighthouse/serverless-events-service",
  "private": true,
  "version": "1.0.0",
  "license": "UNLICENSED",
  "main": "handler.ts",
  "scripts": {
    "deploy": "sls deploy",
    "package": "SLS_DEBUG='*' sls package --stage dev",
    "start": "SLS_DEBUG=* sls offline start --stage dev",
    "test": "echo \"Skipping tests as none\" && exit 0",
    "test:ci": "echo \"Skipping tests as none\" && exit 0",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@lighthouse/serverless-common": "^1.0.0",
    "@lighthouse/serverless-plugin-typescript": "1.1.10-beta-1",
    "aws-xray-sdk": "^2.4.0",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "@types/jest": "^24.0.17",
    "@types/lodash": "^4.14.149",
    "@types/node": "^12.11.7",
    "aws-sdk": "^2.559.0",
    "aws-sdk-mock": "^4.3.1",
    "jest": "^23.x.x",
    "typescript": "^3.7.3"
  }
}
