"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.defaultFooter = defaultFooter;

var _helpers = require("../../../helpers");

function defaultFooter({
  logoUrl,
  timestamp,
  timezone,
  type
}) {
  const datetime = (0, _helpers.getTimezoneDatetime)({
    format: 'Do MMM YYYY',
    showTzAbbr: false,
    timestamp,
    timezone
  }); // eslint-disable-next-line no-unused-vars

  return (0, _helpers.fetchImage)(logoUrl).then(image => _page => ({
    columns: [{
      fit: [80, 30],
      image,
      margin: [60, 20, 0, 0]
    }, {
      alignment: 'right',
      font: 'Gotham',
      text: `${type} | ${datetime}`,
      style: 'small',
      margin: [0, 25, 60, 0]
    }]
  }));
}