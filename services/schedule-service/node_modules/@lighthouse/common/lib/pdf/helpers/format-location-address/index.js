import { getGeometryGeocodedString } from '../../../helpers/get-geometry-geocoded-string';
import { head, join, split, tail } from 'lodash';
export function getFormattedAddress(reverseGeoCoded) {
  var geometry = getGeometryGeocodedString(reverseGeoCoded);
  var splitGeoString = split(geometry, ', ');
  var headText = head(splitGeoString);
  var tailText = join(tail(splitGeoString), ' ');
  var formatted = "".concat(headText, ", ").concat(tailText);
  return formatted;
}
//# sourceMappingURL=index.js.map