import moment from 'moment-timezone';

/**
 * Generates next occurrence interval for non repeat strategy
 */
export function getNextNoRepeat(_ref) {
  var start = _ref.start,
      end = _ref.end,
      timezone = _ref.timezone;
  var nextOccurrenceStart = moment.tz(start, timezone).valueOf();
  var nextOccurrenceEnd = moment.tz(end, timezone).valueOf() - 1;
  if (nextOccurrenceEnd <= nextOccurrenceStart) return;
  return [nextOccurrenceStart, nextOccurrenceEnd];
}
//# sourceMappingURL=getNextNoRepeat.js.map